; Filter configuration for the filtering-follow test.
;
; public.filtered_table       - schema and data both skipped.
; public.data_filtered_table  - schema is copied, data is not.

[exclude-table]
public.filtered_table

[exclude-table-data]
public.data_filtered_table
